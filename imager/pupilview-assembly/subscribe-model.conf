subsystem = IRIS
component = imager.pupilview

subscribe {
  events = [
    {
        subsystem = IRIS
        component = sc.cryoenv
        name      = imgTemperatures
        usage     = "overheating protection"
    }
  ]
}