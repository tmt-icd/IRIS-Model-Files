subsystem = IRIS
component = sci-filter-assembly

subscribe {
  events = [
    {
      subsystem = TCS
      component = pk
      name = pointingStatus
      usage = "Information purposes only for now."
    }
  ]
}

